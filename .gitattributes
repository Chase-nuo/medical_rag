# 统一换行符，避免 Windows/macOS/Linux 协作时整文件变更
* text=auto eol=lf

# 文本文件按纯文本处理，便于 GitHub 上 diff 与高亮
*.py    text eol=lf
*.md    text eol=lf
*.csv   text eol=lf diff
*.txt   text eol=lf
*.json  text eol=lf
*.toml  text eol=lf

# 二进制文件不做换行转换
*.png   binary
*.jpg   binary
*.pdf   binary
*.pyc   binary
